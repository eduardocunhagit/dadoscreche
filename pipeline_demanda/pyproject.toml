[project]
name = "pipeline-demanda-creches"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["numpy>=1.26", "pandas>=2.1", "openpyxl>=3.1"]

[tool.pytest.ini_options]
testpaths = ["testes"]
